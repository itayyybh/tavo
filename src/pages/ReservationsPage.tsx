import { ReservationsView } from '@/features/reservations'

/** Reservations surface — thin page shell composing the feature view. */
export default function ReservationsPage() {
  return <ReservationsView />
}
