import { MobileReservationPage } from '@/features/mobile-reservation'

/** Phone surface (Phase 9) — fast reservation creation, nothing else. */
export default function MobilePage() {
  return <MobileReservationPage />
}
