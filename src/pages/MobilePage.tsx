import { MobileHome } from '@/features/mobile-reservation'

/** Phone surface (Phase 9) — create a reservation or scan today's list. */
export default function MobilePage() {
  return <MobileHome />
}
