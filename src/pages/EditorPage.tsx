/**
 * Layout Editor — the Figma-like restaurant builder (see the `layout-editor` skill).
 * Placeholder shell; built out in Phase 3.
 */
export default function EditorPage() {
  return (
    <section className="mx-auto max-w-3xl px-6 py-16">
      <h1 className="text-2xl font-semibold tracking-tight">Layout Editor</h1>
      <p className="mt-2 text-sm text-[var(--color-muted)]">
        Grid-based drag &amp; drop restaurant builder. Coming in a later phase.
      </p>
    </section>
  )
}
