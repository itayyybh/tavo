import { HistoryView } from '@/features/reservations'

/** History surface — thin page shell composing the archived-reservations view. */
export default function HistoryPage() {
  return <HistoryView />
}
