---
name: performance
description: Performance rules for this app — avoid unnecessary re-renders, memoize expensive components, lazy-load pages, virtualize long lists, optimize canvas rendering. Use when writing render-heavy, list, or canvas code.
---

Always optimize.

- Never re-render unnecessarily.
- Memoize expensive components.
- Lazy load pages.
- Virtualize long lists.
- Optimize canvas rendering.
